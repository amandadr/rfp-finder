# RFP Finder — environment variables (copy to .env and fill in)
# Never commit .env — it is gitignored
#
# To enable AI scoring: set RFP_FINDER_LLM_PROVIDER=openai and OPENAI_API_KEY.
# Run: poetry install -E llm  (installs openai package)

OPENAI_API_KEY=

# Set to "openai" to use AI scoring (otherwise heuristic stub)
RFP_FINDER_LLM_PROVIDER=openai

# Optional: model override
# RFP_FINDER_LLM_MODEL=gpt-4o-mini

# Bids & Tenders: multi-tenant (see rfp-finder tenants for list)
# BIDS_TENDERS_BASE_URL=https://halifax.bidsandtenders.ca  # Legacy: single tenant override
# BIDS_TENDERS_TENANT=halifax                              # Single tenant subdomain
# BIDS_TENDERS_TENANTS=halifax,moncton,bids                # Comma-separated, or "all"
